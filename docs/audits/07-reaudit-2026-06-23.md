# Re-audit 2026-06-23 (pos Wave 10-12)

Branch `redesign/claude-design`. Escopo: verificar 11 fixes Wave 11 + auditar superficies novas (Stripe W10, pgvector W9, refresh/outcome W12). Read-only.

## 1. Verificação dos 11 fixes Wave 11

| # | Fix | Status | Observação |
|---|-----|--------|------------|
| 1 | XSS Zod URL (`safeExternalUrl` + `safeHref`) | **OK** | Nenhum `z.string().url()` novo. `safeHref` em Kanban/EvidenceItem/Radar/Microaction/Report/PortfolioImport. **Issue P2**: `components/NotificationsBell.js:206` `<a href={n.link}>` sem `safeHref` (hoje `link` vem de templates server, mas defesa em camadas faltando). |
| 2 | Rate-limit Redis + fallback Map | **OK** | 11/11 call sites usam `await guardLLM(`. Nenhum bug latente. |
| 3 | TOCTOU `enforceUsage` Serializable | **OK** | Todas rotas LLM chamam `enforceUsage` no início. Refresh W12 wirou ok. |
| 4 | Budget per-user `DAILY_COST_CAP_USD` | **NÃO WIRADO (P1)** | `trackTokenUsage` e `checkDailyBudget` definidos em `lib/billing/enforce.js` mas **0 call sites** em `app/api/**`. Pendência W11 segue 100% aberta — user Free pode esgotar Sonnet 4.6 em loop até quota Anthropic estourar, `costUsd` em UsageMeter fica 0. |
| 5 | AuditLog em ações críticas | **PARCIAL** | Bom: login/logout/createUser, CV upload, CV redact, account delete, data export, webhook (BILLING_*), outcome, refresh. Faltam: `CONSENT_GRANTED/REVOKED` (enum + 0 callers), `PROFILE_UPDATED` em `app/(app)/conta/page.js` server actions, `SECURITY_RATE_LIMIT_HIT`+`SECURITY_BUDGET_EXCEEDED` (0 callers). |
| 6 | `Profile.rawCv` TTL 90d | **BUG (P0)** | `/api/cv/upload` e `/api/analyze` setam `rawCvExpiresAt = now + 90d`. **`/api/linkedin/parse:89-108` grava `linkedinRaw` sem nunca setar `rawCvExpiresAt`**. O cron `redact-cv` filtra `WHERE rawCvExpiresAt: { lt: now, not: null }` — perfis LinkedIn-only ficam com `linkedinRaw` PERMANENTE, viola LGPD. Refresh corretamente NÃO persiste rawCv (linha 312 é in-memory). |
| 7 | `migrate deploy` fora do build | **OK** | `build` = `prisma generate && next build`. `db:migrate` separado. DEPLOY.md explica. |
| 8 | Transação `/opportunities` | **OK** | `deleteMany` + `createMany` sem $transaction (linha 293). |
| 9 | Auth rate-limit (`enforceAuthRate`) | **OK** | Em Resend e Nodemailer. LinkedIn é OAuth. Credentials só em dev. |
| 10 | Middleware sync (`auth-protected-paths`) | **OK** | SSoT importada nos dois. `/api/me/` cobre outcome; `/api/profile/` cobre refresh. |
| 11 | Chat ownership | **OK** | `/api/chat:54-79` carrega perfil+gaps do DB via session. Validator strict bloqueia body. |

## 2. Novas superfícies de ataque

### Stripe (Wave 10)
- **Webhook HMAC**: `constructEvent(rawBody, sig, secret)` com `req.text()` (App Router não pré-parsa). Audit `SECURITY_INVALID_WEBHOOK` em falha. OK.
- **Idempotência**: `BillingEvent.stripeEventId` UNIQUE; P2002 = 200 sem reaplicar. OK.
- **Customer Portal**: `customerId` resolvido por `session.user.id`. Sem IDOR.
- **Checkout metadata**: `userId` vem de `auth()`, `planId` vem do body mas é re-validado via whitelist `getPlan()` que falha-closed pra "free". OK.
- **Test mode keys**: sem fallback default; chave faltando = 503. OK.
- **Race "concluído após cancelar conta"**: `User.delete` cascade apaga Subscription, `BillingEvent` fica `userId=null` (SetNull). Webhook posterior cai em `findUnique({ stripeSubscriptionId })` que retorna null → no-op. OK.
- **Novo issue P2**: `BillingEvent.payload` armazena `event.data.object` cru (email/metadata Stripe) indefinidamente. Export sanitiza, mas DB fica com PII. Recomenda cron de redaction > 1 ano.

### pgvector (Wave 9)
- **`$queryRaw` vector**: `lib/knowledge/retrieval.js:108-122` usa template tag (parametrizado). `${topic}` parametrizado; `${limit*2}` é number. OK.
- **Embedding via Voyage**: texto do CV vai pro Voyage; embeddings não executam instruções. OK.
- **KnowledgeChunk writes**: somente via `scripts/ingest-knowledge.mjs` local. Atacante autenticado não escreve. OK.

### Refresh (Wave 12)
- **`completedHabilidades` no prompt**: `lib/prompts.js:55-58` aplica `sanitize()` em cada item (substitui `"""`, slice 20). OK.
- **Bonus 25 pts**: cap dimensão 15 + total 25. Fonte é `previousSnapshot.gaps` (DB do user). Marcar como done via `/api/gaps/[id]/complete` é idempotente (`alreadyDone`), não dá pra farmar 2x. OK.

### Outcome (Wave 12)
- **`scoreAtTime` server-side**: validator não aceita; vem de `findFirst({ where: { userId } })`. OK.
- **Audit OUTCOME_REPORTED**: meta sem `evidence` raw. OK.

## 3. Top 10 issues NÃO fixadas

| # | Sev | Local | Issue | Fix |
|---|-----|-------|-------|-----|
| 1 | **P0** | `app/api/linkedin/parse/route.js:89-108` | LinkedIn-only profile nunca tem `rawCvExpiresAt` setado → cron pula → `linkedinRaw` permanente. Viola LGPD | Setar `rawCvExpiresAt` no upsert (mesma const de analyze). |
| 2 | **P1** | `lib/billing/enforce.js:228,274` | `trackTokenUsage`+`checkDailyBudget` definidos, 0 callers → cost cap não funciona | Cada rota LLM: pós `completeJSON`, chamar `trackTokenUsage`. Pré: `checkDailyBudget`. |
| 3 | **P1** | `prisma/schema.prisma` enum | `CONSENT_GRANTED/REVOKED`, `SECURITY_RATE_LIMIT_HIT/BUDGET_EXCEEDED`, `OUTCOME_SURVEY_DECLINED` com 0 callers | Disparar em `tooMany()`, `checkDailyBudget` falha, UI consent futura. |
| 4 | **P1** | `app/(app)/conta/page.js:36-98` | 3 server actions editam User/Profile sem `PROFILE_UPDATED` | `await audit({ action: "PROFILE_UPDATED", ... })` em cada. |
| 5 | **P1** | `lib/data-export.js` | Export LGPD não inclui AuditLog do próprio user — falta transparência | Adicionar `prisma.auditLog.findMany({ where: { userId }, take: 1000 })`. |
| 6 | **P2** | `components/NotificationsBell.js:206` | `<a href={n.link}>` sem `safeHref` — defesa em camadas | `safeHref(n.link)`. |
| 7 | **P2** | `app/api/billing/webhook/route.js:82` | `BillingEvent.payload` armazena raw indefinidamente (email Stripe) | Cron mensal redacta payload > 1 ano. |
| 8 | **P2** | `lib/auth.js:185-191` | Provider `Credentials` dev sem `enforceAuthRate` | Low-risk (dev only); adicionar em `authorize()`. |
| 9 | **P2** | `lib/audit.js:62-72` | `audit()` falha silenciosa via `console.error` | Hook Sentry `captureException`. |
| 10 | **P2** | `app/api/billing/webhook/route.js:194-220` | `handleSubscriptionUpserted` 2 queries; race se Stripe entrega `created`/`updated` fora de ordem | `prisma.subscription.upsert` em path único. |

## 4. Pendências documentadas continuam abertas

- **`trackTokenUsage` wiring**: 0 callers confirmado (issue #2). W11 deixou padrão em `enforce.js:14-18`, nenhuma rota seguiu.
- **CONSENT_GRANTED/REVOKED**: UI granular ainda não existe (só banner PostHog) — espera W13.
- **PROFILE_UPDATED em /conta**: 3 server actions sem audit (issue #4).
- **Alerting Sentry pico SECURITY_***: só `SECURITY_INVALID_WEBHOOK` é emitido. Sem alerta configurado (issue #9 cobre o canal).

## 5. CI security gate

- `.github/workflows/ci.yml:46-49`: `npm audit --audit-level=high --omit=dev` bloqueia high+. OK.
- Sem Snyk/GitGuardian/OWASP-dep — Dependabot+npm audit cobrem caminho feliz pra dev solo.
- Dependabot bloqueia majors: prisma, @prisma/client, next, next-auth, @auth/prisma-adapter, react, react-dom, stripe. Sem bloqueio em @upstash/redis, @sentry/nextjs, zod (minor risco). OK.

## 6. Threat model emergente

- **Free tier autenticado**: refresh bombing defendido por rate-limit 10/min + enforceUsage atomico (3/mês). Mas `trackTokenUsage` ausente = budget Anthropic global drena sem reflexo em `costUsd`. Cobertura quebra.
- **IDOR**: padrão 2-step ownership em gaps/plan-items/evidence/applications/tailored-cvs. Defendido.
- **Supply chain PR**: Dependabot blocks majors; CI roda npm audit. `e2e.yml` não revisado — recomendo verificar se usa `pull_request_target` (fork secrets leak).
- **Interno futuro**: nenhum endpoint expõe AuditLog. Próprio user não vê o seu (issue #5). Pre-time, risco aceito; LGPD merece o endpoint.

## 7. Recomendações priorizadas

### P0 (essa semana)
1. Fix `linkedin/parse` TTL (#1) — 1 linha, viola LGPD em prod.

### P1 (próxima Wave)
2. Wire `trackTokenUsage`+`checkDailyBudget` em 5 rotas LLM (#2). Dispara `SECURITY_BUDGET_EXCEEDED`.
3. Audit `PROFILE_UPDATED` em /conta server actions (#4).
4. Incluir AuditLog em data-export (#5).
5. Dispatch `SECURITY_RATE_LIMIT_HIT` em `tooMany()` (#3 parcial).

### P2 (backlog)
6. `safeHref` em NotificationsBell (#6).
7. Cron redaction `BillingEvent.payload` > 1 ano (#7).
8. Sentry hook em `audit()` falha (#9).
9. Upsert único em `handleSubscriptionUpserted` (#10).

Nenhum achado das audits 01-05 re-aparece como NOVO. Pendência conhecida (budget per-user) segue 100% aberta apesar de W11 ter declarado o helper.
